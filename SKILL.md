---
name: wechat-chat-export
description: 将 Windows 微信 4.x 本地数据库中使用者本人的聊天记录导出为可浏览的 HTML 时间线、CSV、TXT。首次捕获密钥时，脚本会完整说明并征求同意后才获取第三方组件，不同意则立即终止。适用于"导出微信聊天记录 / 导出与某人的聊天 / 微信记录存档"等场景。触发词：微信导出、聊天记录导出、导出微信聊天、把微信记录导出来、wechat export。
agent_created: true
version: 1.1.2
author: 天工创新坊
license: CC BY 4.0
display_name: "微信聊天记录导出"
display_name_en: WeChat Chat Export
trigger: ["导出微信聊天记录", "微信导出", "聊天记录导出", "把微信记录导出来", "微信记录存档"]
description_zh: "将 Windows 微信 4.x 本地聊天记录导出为 HTML 时间线 / CSV / TXT（获取密钥组件前先征求使用者同意）"
description_en: "Export Windows WeChat 4.x local chat history to HTML/CSV/TXT (asks for user consent before fetching the key-capture component)"
category: productivity
permissions: ["本地文件读取", "本地文件写入", "经用户确认后联网下载组件"]
---

# 微信聊天记录导出（Windows 微信 4.x）

将本机微信 4.x 本地数据库中**使用者本人**的聊天记录导出，生成 HTML（微信风格时间线）、CSV（表格）、TXT（纯文本）三种格式，便于交由 AI 分析或留底归档。

## 安全边界

- **数据不出本机**：导出与解密全过程在本机完成，脚本不上传任何聊天数据、密钥或文件。
- **不附带第三方组件，下载前须征求同意**：技能包不含任何二进制组件。首次捕获密钥时，若本机没有捕获组件，脚本会将「下载什么、用途、来源、仅下载一次」完整打印并征求使用者同意——**同意才下载，不同意立即终止**，不做任何更改。
- **操作微信前先征得同意**：捕获密钥需重启一次微信并在手机上确认登录，脚本会先行说明、征得使用者同意后执行。
- **权限最小化**：仅读写使用者指定的本地目录，不涉及系统配置、启动项与后台进程。

## 一、有什么用处

- 将使用者本人（单聊/群聊）的聊天记录完整导出，交由 AI 分析（沟通复盘、谈判留底、关系梳理等）
- 将重要对话留底存档，随时可查、可检索
- 生成可按天浏览的网页版聊天记录，呈现形式与微信一致

## 二、为什么要用

微信聊天记录存放于本地加密数据库，微信本身未提供一键导出功能。手动逐条复制效率低，截图交由 AI 分析亦受上下文额度限制。本技能将一条**已验证跑通的路径**整理为清晰步骤，配备现成脚本，并附踩坑记录，便于使用者一次走对，节省时间与额度。

## 三、使用方法（四步工作流）

### 前置条件

- 操作系统为 Windows；微信已安装并登录过，数据目录保留在磁盘
- 导出**不要求微信正在运行**（具备密钥与离线主库即可）
- 若本地尚无密钥，则需微信**运行并登录**，并接受一次「重启微信 + 手机确认登录」流程
- Python 依赖：`pycryptodome`、`zstandard`（`pip install pycryptodome zstandard`）
- 技能自带脚本：`scripts/capture_key.py`（捕获密钥）、`scripts/export_chat.py`（解密导出）

### 第 1 步：确定数据根目录与目标账号

微信数据根目录下每个账号一个文件夹（形如 `wxid_xxx_24bb\db_storage\`），常见位置如 `文档\xwechat_files`（具体磁盘位置因安装设置而异，可用 `--data-root` 指定）。

**关键事实**：不同账号目录使用不同的数据库密钥，一把密钥只能解锁一个账号目录。先列出目录，确认存在哪些账号：

```powershell
Get-ChildItem '<微信数据根目录>\xwechat_files' -Directory
```

### 第 2 步：取得数据库密钥（二选一）

**优先复用已捕获密钥**：若此前导出过，先查找使用者保存的 `key.txt`（64 位 hex）。取得后跳过本步，直接进入第 3 步（第 3 步会做 HMAC 校验，密钥不正确会立即报错）。

**无密钥时运行捕获脚本**。原理：微信仅在启动登录早期调用 `SetDBKey`，因此须「先关闭微信 → 脚本先挂 Hook → 再登录」：

1. **智能体须先向使用者转述以下披露信息并取得明确同意**：
   - 本机没有密钥捕获组件时，脚本需从公开来源下载一次第三方工具（WXexport-tool v1.2.0 的 `wx_key.dll`），用于在本机捕获微信登录时的数据库密钥；
   - 仅在本机使用，密钥只写入本机文件，不上传、不外传；仅下载一次，之后长期复用；
   - 使用者**不同意则不下载、流程终止**，不做任何更改。
2. 使用者同意后运行：
   ```powershell
   python "<skill>\scripts\capture_key.py" --wechat-exe "<微信安装路径>\Weixin.exe" --out <保存目录> --close-first --yes-download
   ```
   （`--yes-download` 表示使用者已知情同意；**未取得同意时不应加此参数**。使用者本人在终端交互运行时可不加，脚本会当面询问 y/n。）
3. 请使用者完成手机确认；待脚本打印「捕获成功」，密钥写入 `<保存目录>\key.txt`。
4. 若超时：通常为登录完成早于 Hook 安装。**不宜反复扫码**，关闭微信后重跑一次（先让脚本就绪再登录）。

> 使用者拒绝下载时：如实告知流程已终止，可建议其改用其他方式取得本人密钥后再用第 3 步导出。不反复追问、不代替使用者做决定。

### 第 3 步：导出某联系人聊天记录

```powershell
python "<skill>\scripts\export_chat.py" `
  --data-root "<微信数据根目录>\xwechat_files" `
  --key-file "<使用者的 key.txt 路径>" `
  --target 联系人备注或昵称 `
  --out-dir <输出目录>
```

脚本自动完成：HMAC 校验密钥（仅解使用者本人的账号目录）→ 找出可解锁的账号目录 → 定位联系人（备注/昵称）→ 解密所有 `message_<n>.db` → 合并该会话全部消息 → 解压内容 → 输出 HTML/CSV/TXT 到使用者指定的输出目录，并交叉核对完整性。

参数提示：
- 同名备注多个时，脚本会列出候选，要求以 `--target-wxid` 精确指定
- 若本人 wxid 推导有误，可用 `--self-wxid wxid_xxx` 覆盖
- 联系人不在当前可解锁账号时，报「未找到该联系人的消息」——多半消息位于另一个账号目录（另一把密钥），需先登录该账号并捕获其密钥，再单独导出

### 第 4 步：交付与汇报

- 以绝对路径告知三个产物：`<名称>-微信聊天记录-<日期>.html/.csv/.txt`
- 汇报：消息总数、时间范围、最后消息时间、类型分布
- 说明内容边界：以**主库文件**为准（微信已改写 WAL，未并入）；图片/语音/视频默认以类型标签占位

## 四、常见问题速查

1. **避免用新账号密钥解锁旧账号目录**，否则将全部 HMAC 失败——这是"已更换账号"的信号，而非脚本故障。
2. **发送者映射逐库可能翻转**：`real_sender_id` 指向 `Name2Id` 的 rowid，须按每个库各自的 `Name2Id` 判定，不应硬编码 1/2。
3. **消息内容可能被 ZSTD 压缩**，特征是开头魔数 `28 b5 2f fd`。
4. **微信运行时数据库文件被独占**：读取须使用带共享权限的 `CreateFileW`（脚本已内置）。
5. **不宜反复重启微信**：每次重启都要求手机确认；捕获应一次就绪后再登录。

更完整的解密参数与库结构说明，见 `references/technical-notes.md`。

## 五、有问题怎么办

- **密钥不正确 / 全部 HMAC 失败**：多为取错账号的密钥，或账号已更换；先确认该密钥对应哪个账号目录。
- **找不到联系人消息**：消息位于另一个账号目录，换用该密钥再导出。
- **捕获超时**：登录完成早于 Hook 安装，关闭微信后重跑，不宜反复扫码。
- **输出缺少最新若干条**：微信改写 WAL，主库未合并最新帧，属正常情况，以主库为准。
- **脚本报缺依赖**：执行 `pip install pycryptodome zstandard` 补齐。
- **提示"已终止（使用者不同意下载）"**：使用者选择不同意，属正常终止；如需继续，可重新运行并同意，或手动放置组件。

## 六、合规与免责声明（发布/分享时须附）

> 本技能仅用于处理**使用者本人**在本机合法持有的聊天数据，使用前应征得数据涉及各方的同意。请遵守所在国家或地区的法律法规与微信平台规则。导出内容含个人隐私，请妥善保管，不得外泄、不得用于侵害他人权益的用途。因使用本技能产生的一切后果由使用者自行承担。
>
> **组件来源致谢**：脚本在征得使用者同意后获取的密钥捕获组件 `wx_key.dll` 取自开源项目 WXexport-tool（v1.2.0，作者 Ray0612，github.com/Ray0612/WeChat-Export-Tool），感谢原作者的开放分享。该组件仅在本机使用，密钥只写入本机文件，不上传、不外传。
