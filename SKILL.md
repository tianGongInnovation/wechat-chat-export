---
name: wechat-chat-export
description: 把 Windows 微信 4.x 本地数据库里的聊天记录解密并导出，生成可浏览的 HTML 时间线、CSV、TXT。用于"导出微信聊天记录 / 把和某人的聊天保存下来 / 解密微信数据库 / 获取微信数据库密钥"等场景，也适用于按联系人整理本机微信历史消息、生成可搜索文件或网页。触发词：微信导出、聊天记录导出、导出微信聊天、微信聊天记录、把微信记录导出来、解密微信数据库、wechat export。
agent_created: true
version: 1.0.0
author: 天工创新坊
license: CC BY 4.0
display_name: "微信聊天记录导出"
display_name_en: WeChat Chat Export
trigger: ["导出微信聊天记录", "微信导出", "聊天记录导出", "把微信记录导出来", "微信数据库"]
description_zh: "导出 Windows 微信 4.x 本地聊天记录（HTML 时间线 / CSV / TXT）"
description_en: "Export Windows WeChat 4.x local chat history to HTML/CSV/TXT"
category: productivity
---

# 微信聊天记录导出（Windows 微信 4.x）

把本机微信 4.x 的本地数据库解密，导出某个联系人的全部聊天记录，生成 HTML（微信风格时间线）、CSV（表格）、TXT（纯文本）三种格式，方便喂给 AI 分析或留底归档。

## 一、有什么用处

- 把和某个人（单聊/群聊）的聊天记录完整导出来，交给 AI 分析（沟通复盘、谈判留底、关系梳理等）
- 把重要对话留底存档，随时可查、可搜
- 生成可按天浏览的网页版聊天记录，看起来和微信一样

## 二、为什么要用

微信聊天记录存在加密数据库里，AI 读不到，人也很难一键导出。手动一条条复制太累，截图喂 AI 又烧额度。更麻烦的是：很多人不知道"正确的路怎么走"，在错误的方法上绕远（比如内存扫描、反复重启微信），浪费大量时间。

本技能把一条**已验证跑通的正确路线**整理成四步，配好现成脚本，附上踩坑记录，帮你一次走对、省时省额度。

## 三、怎么用（四步工作流）

### 前置条件

- 操作系统 Windows；微信需已安装并登录过，数据目录保留在磁盘
- 导出**不要求微信正在运行**（有密钥 + 离线主库即可）
- 若本地还没有密钥，则需要微信**运行并登录**，并接受一次「重启微信 + 手机确认登录」的流程
- Python 依赖：`pycryptodome`、`zstandard`（`pip install pycryptodome zstandard`）
- 技能自带两个脚本：`scripts/capture_key.py`（捕获密钥）、`scripts/export_chat.py`（解密导出）
- 首次捕获密钥时，若本机没有密钥捕获组件，脚本会自动联网获取所需的第三方工具（一次性），请保持网络可用

### 第 1 步：确定数据根目录与目标账号

微信数据根目录通常为 `E:\微信聊天信息\xwechat_files`，其下每个账号一个文件夹：`wxid_xxx_24bb\db_storage\`。

**关键事实**：不同账号目录使用不同的数据库密钥，某把密钥只能解锁一个账号目录。先列目录确认有哪些账号：

```powershell
Get-ChildItem 'E:\微信聊天信息\xwechat_files' -Directory
```

### 第 2 步：取得数据库密钥（二选一）

**优先复用已捕获密钥**：若之前导出过，先找保存的 `key.txt`（64 位 hex）。拿到后跳过本步，直接进第 3 步（第 3 步会做 HMAC 校验，密钥不对会立刻报错）。

**没有密钥则运行捕获脚本**。原理：微信只在启动登录早期调用 `SetDBKey`，必须「先关微信 → 脚本先挂 Hook → 再登录」：

1. 先向使用者说明：需要重启微信，并在手机上确认登录（或扫码），征得同意后执行。
2. 运行：
   ```powershell
   python "<skill>\scripts\capture_key.py" --wechat-exe "D:\WeiXin\Weixin.exe" --out <保存目录> --close-first
   ```
3. 让使用者完成手机确认；等脚本打印「捕获成功」，密钥写入 `<保存目录>\key.txt`。
4. 若超时：通常是登录完成早于 Hook 安装。**不要反复扫码**，关闭微信后重跑一次（先让脚本就绪再登录）。

> 关于密钥捕获组件：本技能**不附带**第三方组件，也**不需要使用者手动下载**。运行捕获脚本时，脚本会自动检测本机是否已有，没有则**自动下载第三方密钥捕获工具并使用**，全程自动执行、只给提示，不打断操作。首次需联网获取（仅一次，之后复用）。

### 第 3 步：导出某联系人聊天记录

```powershell
python "<skill>\scripts\export_chat.py" `
  --data-root "E:\微信聊天信息\xwechat_files" `
  --key-file "<你的 key.txt 路径>" `
  --target 联系人备注或昵称 `
  --out-dir <输出目录>
```

脚本自动完成：HMAC 校验密钥 → 找出能解锁的账号目录 → 定位联系人（备注/昵称）→ 解密所有 `message_<n>.db` → 合并该会话全部消息 → 解压内容 → 输出 HTML/CSV/TXT，并交叉核对完整性。

参数提示：
- 同名备注多个时，脚本会列出候选，要求用 `--target-wxid` 精确指定
- 若本人 wxid 推导有误，用 `--self-wxid wxid_xxx` 覆盖
- 联系人不在当前可解锁账号时，报「未找到该联系人的消息」——多半消息在另一个账号目录（另一把密钥），需先登录那个账号并捕获其密钥，再单独导出

### 第 4 步：交付与汇报

- 以绝对路径告知三个产物：`<名称>-微信聊天记录-<日期>.html/.csv/.txt`
- 汇报：消息总数、时间范围、最后消息时间、类型分布
- 说明内容边界：以**主库文件**为准（微信魔改过 WAL，未并入）；图片/语音/视频默认以类型标签占位

## 四、关键陷阱速查（实战踩坑，最省时间的部分）

1. **别拿新账号密钥解旧账号目录**，会全部 HMAC 失败——这是"换账号了"的信号，不是脚本坏。
2. **发送者映射逐库可能翻转**：`real_sender_id` 指向 `Name2Id` 的 rowid，必须按每个库自己的 `Name2Id` 判定谁是谁，禁止硬编码 1/2。
3. **消息内容可能被 ZSTD 压缩**，特征是开头魔数 `28 b5 2f fd`。
4. **微信运行时数据库文件被独占**：读取必须用带共享权限的 `CreateFileW`（脚本已内置）。
5. **别用 Frida 大范围内存扫描当首选**：不同版本偏移差异大，且反复附加可能让微信自我保护退出——已实测 1700 多个候选全未命中。
6. **别反复重启微信**：每次重启都要求手机确认；捕获务必一次就绪再登录。

更完整的解密参数、库结构、失败路线，见 `references/technical-notes.md`。

## 五、有问题怎么办

- **密钥不对/全 HMAC 失败**：多半是拿错账号的密钥，或账号换过；先确认这把 key 对应哪个账号目录。
- **找不到联系人消息**：消息在另一个账号目录，换那把密钥再导。
- **捕获超时**：登录完成早于 Hook 装好，关微信重跑，别反复扫码。
- **输出缺最新几条**：微信魔改 WAL，主库没合并最新帧，属正常，以主库为准。
- **脚本报缺依赖**：`pip install pycryptodome zstandard` 补上。

## 六、免责声明（发布/分享时必附）

> 本技能仅用于处理**使用者本人**合法持有的聊天数据。请遵守所在国家/地区的法律法规和微信平台规则。解密密钥提取与数据库解密涉及对微信客户端的本地处理，可能带来账号风险，由使用者自行评估并承担后果。导出内容含个人隐私，请妥善保管，不得外泄。本技能不附带第三方组件，所需的第三方密钥捕获工具由脚本在使用时自动获取，仅在本机使用。
